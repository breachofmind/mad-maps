# --- Server ---
PORT=4000
CLIENT_ORIGIN=http://localhost:5173

DATABASE_URL=postgres://mapinski:mapinski@localhost:5433/mapinski

SESSION_SECRET=change-me

# Google Cloud project credentials (OAuth 2.0 Client — Web application)
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_CALLBACK_URL=http://localhost:4000/api/auth/google/callback

# Google Maps Platform API key (Places/Geocoding) — used server-side only (Phase 6)
GOOGLE_MAPS_API_KEY=

# --- Client (Vite, must be prefixed VITE_) ---
VITE_MAPBOX_ACCESS_TOKEN=
VITE_API_BASE_URL=http://localhost:4000
